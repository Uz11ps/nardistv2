# Использование готовых образов из GitHub Container Registry

## Проблема: Медленная сборка на сервере

Сборка Docker образов на сервере может занимать 10+ минут из-за установки npm зависимостей.

## Решение: Использование готовых образов

GitHub Actions автоматически собирает образы при каждом push в `main`. Вместо сборки на сервере можно использовать готовые образы.

## Настройка

### Шаг 1: Обновите .env файл на сервере

```bash
cd /opt/Nardist
nano .env
```

Добавьте или измените следующие строки:

```env
# Используйте готовые образы из GitHub Container Registry
BACKEND_IMAGE=ghcr.io/uz11ps/nardist-backend:latest
FRONTEND_IMAGE=ghcr.io/uz11ps/nardist-frontend:latest
```

### Шаг 2: Войдите в GitHub Container Registry (опционально)

Если образы приватные, нужно войти:

```bash
echo $GITHUB_TOKEN | docker login ghcr.io -u USERNAME --password-stdin
```

Но обычно для публичных репозиториев это не требуется.

### Шаг 3: Запустите деплой

```bash
cd /opt/Nardist
./deploy.sh
```

Теперь скрипт автоматически:
1. Попытается скачать готовые образы из GitHub Container Registry
2. Если образы не найдены, соберет их локально

## Преимущества

- ⚡ **Быстро**: Скачивание образов занимает 1-2 минуты вместо 10+ минут сборки
- 🔄 **Автоматически**: Образы обновляются при каждом push в `main` через GitHub Actions
- 💰 **Экономия ресурсов**: Не нагружает сервер сборкой

## Проверка доступности образов

Проверьте, что образы доступны:

```bash
# Проверка backend образа
docker pull ghcr.io/uz11ps/nardist-backend:latest

# Проверка frontend образа
docker pull ghcr.io/uz11ps/nardist-frontend:latest
```

## Если образы не найдены

Если образы еще не собраны в GitHub Actions:

1. Сделайте любой коммит и push в `main`
2. Дождитесь завершения GitHub Actions workflow
3. Проверьте, что образы появились в GitHub Container Registry
4. Затем используйте их на сервере

## Альтернатива: Локальная сборка

Если хотите собирать локально (медленнее, но больше контроля):

```env
# В .env файле оставьте пустыми или используйте локальные имена
BACKEND_IMAGE=nardist-backend:latest
FRONTEND_IMAGE=nardist-frontend:latest
```

Скрипт автоматически определит, что нужно собрать локально.

