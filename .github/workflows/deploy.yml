name: Deploy to Production

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend image
        uses: docker/build-push-action@v5
        continue-on-error: false
        with:
          context: ./backend
          push: true
          tags: ghcr.io/uz11ps/nardist-backend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64

      - name: Build and push frontend image
        uses: docker/build-push-action@v5
        continue-on-error: false
        with:
          context: ./frontend
          push: true
          tags: ghcr.io/uz11ps/nardist-frontend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64

      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: 22
          timeout: 60s
          command_timeout: 900s
          script: |
            cd /opt/Nardist
            
            # Определяем команду docker compose
            if command -v docker &> /dev/null && docker compose version &> /dev/null; then
              DOCKER_COMPOSE="docker compose"
            elif command -v docker-compose &> /dev/null; then
              DOCKER_COMPOSE="docker-compose"
            else
              echo "❌ Error: docker compose not found!"
              exit 1
            fi
            
            # Обновляем код (принудительно берем версию из репозитория, локальные изменения не важны)
            git fetch origin
            git reset --hard origin/main || git reset --hard origin/master
            
            # Создаем или обновляем .env файл из GitHub Secrets
            echo "📝 Creating/updating .env file from GitHub Secrets..."
            
            # Используем переменные окружения для безопасной передачи секретов
            {
              echo "# Database"
              echo "POSTGRES_USER=${POSTGRES_USER:-nardist}"
              echo "POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}"
              echo "POSTGRES_DB=${POSTGRES_DB:-nardist_db}"
              echo ""
              echo "# Backend"
              echo "JWT_SECRET=${{ secrets.JWT_SECRET }}"
              echo "TELEGRAM_BOT_TOKEN=${{ secrets.TELEGRAM_BOT_TOKEN }}"
              echo "NODE_ENV=production"
              echo "PORT=3000"
              echo ""
              echo "# Frontend"
              echo "VITE_API_URL=${VITE_API_URL:-https://nardist.online}"
              echo "VITE_WS_URL=${VITE_WS_URL:-https://nardist.online}"
              echo "FRONTEND_URL=${FRONTEND_URL:-https://nardist.online}"
              echo ""
              echo "# Domain"
              echo "DOMAIN_NAME=${DOMAIN_NAME:-nardist.online}"
              echo "SSL_EMAIL=${{ secrets.SSL_EMAIL }}"
              echo ""
              echo "# Docker Images"
              echo "BACKEND_IMAGE=ghcr.io/uz11ps/nardist-backend:latest"
              echo "FRONTEND_IMAGE=ghcr.io/uz11ps/nardist-frontend:latest"
            } > .env
            
            echo "✅ .env file created/updated"
            
            # Проверяем что критичные переменные установлены (проверяем в созданном файле)
            if ! grep -q "POSTGRES_PASSWORD=.*[^[:space:]]" .env || ! grep -q "JWT_SECRET=.*[^[:space:]]" .env; then
              echo "⚠️  WARNING: Some critical secrets may be missing in GitHub Secrets!"
              echo "⚠️  Please verify that .env file contains: POSTGRES_PASSWORD, JWT_SECRET, TELEGRAM_BOT_TOKEN, SSL_EMAIL"
            fi
            
            # Загружаем переменные окружения
            set -a
            source .env
            set +a
            
            # Используем готовые образы из GitHub Container Registry
            export BACKEND_IMAGE=ghcr.io/uz11ps/nardist-backend:latest
            export FRONTEND_IMAGE=ghcr.io/uz11ps/nardist-frontend:latest
            
            # Скачиваем базовые образы (с sudo если нужно)
            sudo $DOCKER_COMPOSE -f docker-compose.prod.yml pull postgres redis nginx certbot 2>/dev/null || \
            $DOCKER_COMPOSE -f docker-compose.prod.yml pull postgres redis nginx certbot || true
            
            # Пытаемся скачать готовые образы приложений
            BACKEND_PULLED=false
            FRONTEND_PULLED=false
            
            # Пытаемся скачать образы (с sudo если нужно)
            if sudo docker pull ${BACKEND_IMAGE} 2>/dev/null || docker pull ${BACKEND_IMAGE} 2>/dev/null; then
              BACKEND_PULLED=true
              echo "✅ Backend image pulled successfully"
            else
              echo "⚠️  Backend image not found, will build"
            fi
            
            if sudo docker pull ${FRONTEND_IMAGE} 2>/dev/null || docker pull ${FRONTEND_IMAGE} 2>/dev/null; then
              FRONTEND_PULLED=true
              echo "✅ Frontend image pulled successfully"
            else
              echo "⚠️  Frontend image not found, will build"
            fi
            
            # Останавливаем системный nginx (Ispmanager) если он занимает порты 80/443
            systemctl stop nginx 2>/dev/null || true
            systemctl disable nginx 2>/dev/null || true
            # Проверяем что порты свободны
            if lsof -i :80 2>/dev/null | grep -v docker; then
              echo "⚠️  Port 80 is occupied by non-docker process, attempting to free it..."
              lsof -ti :80 | xargs kill -9 2>/dev/null || true
            fi
            if lsof -i :443 2>/dev/null | grep -v docker; then
              echo "⚠️  Port 443 is occupied by non-docker process, attempting to free it..."
              lsof -ti :443 | xargs kill -9 2>/dev/null || true
            fi
            
            # Безопасная остановка через docker compose (останавливает все контейнеры проекта)
            echo "🛑 Stopping existing containers..."
            sudo $DOCKER_COMPOSE -f docker-compose.prod.yml down --remove-orphans 2>/dev/null || \
            $DOCKER_COMPOSE -f docker-compose.prod.yml down --remove-orphans 2>/dev/null || true
            
            # Принудительно останавливаем и удаляем контейнеры по имени (на случай если они запущены не через compose)
            echo "🧹 Force removing old containers by name..."
            for container in nardist_nginx_prod nardist_postgres_prod nardist_redis_prod nardist_backend_prod nardist_frontend_prod nardist_certbot; do
              # Принудительно удаляем (останавливает и удаляет одновременно)
              sudo docker rm -f $container 2>/dev/null || docker rm -f $container 2>/dev/null || true
            done
            
            # Удаляем все контейнеры проекта по префиксу (включая те что имеют ID в имени)
            echo "🧹 Removing containers by prefix..."
            sudo docker ps -a --filter "name=nardist_" --format "{{.ID}}" 2>/dev/null | while read id; do
              sudo docker rm -f $id 2>/dev/null || docker rm -f $id 2>/dev/null || true
            done || true
            
            # Также удаляем через docker ps -a и grep для надежности
            sudo docker ps -a 2>/dev/null | grep -E "nardist_(nginx|postgres|redis|backend|frontend|certbot)_prod" | awk '{print $1}' | while read id; do
              sudo docker rm -f $id 2>/dev/null || docker rm -f $id 2>/dev/null || true
            done || true
            
            # Ждем немного чтобы система освободила ресурсы
            sleep 2
            
            # Открываем порты в firewall (если используется ufw)
            # ВАЖНО: Сначала ВСЕГДА открываем SSH (22), чтобы не заблокировать соединение!
            if command -v ufw &> /dev/null; then
              # Сначала добавляем все правила (особенно SSH!)
              ufw allow 22/tcp 2>/dev/null || true
              ufw allow 80/tcp 2>/dev/null || true
              ufw allow 443/tcp 2>/dev/null || true
              # Включаем только если еще не включен
              if ! ufw status | grep -q "Status: active"; then
                ufw --force enable 2>/dev/null || true
              fi
            fi
            
            # Запускаем контейнеры (только build если образы не скачаны)
            # Используем sudo если нужно, иначе обычную команду
            if [ "$BACKEND_PULLED" = true ] && [ "$FRONTEND_PULLED" = true ]; then
              echo "🚀 Using pre-built images, skipping build"
              sudo $DOCKER_COMPOSE -f docker-compose.prod.yml up -d --force-recreate --no-build --pull never 2>/dev/null || \
              $DOCKER_COMPOSE -f docker-compose.prod.yml up -d --force-recreate --no-build --pull never
            else
              echo "🔨 Building missing images..."
              sudo $DOCKER_COMPOSE -f docker-compose.prod.yml up -d --force-recreate --build 2>/dev/null || \
              $DOCKER_COMPOSE -f docker-compose.prod.yml up -d --force-recreate --build
            fi
            
            # Ждем готовности сервисов
            sleep 10
            
            # Явно запускаем nginx если он не запущен
            if ! sudo docker ps 2>/dev/null | grep -q nardist_nginx_prod && ! docker ps 2>/dev/null | grep -q nardist_nginx_prod; then
              echo "⚠️  Nginx not running, starting it..."
              sudo $DOCKER_COMPOSE -f docker-compose.prod.yml up -d nginx 2>/dev/null || \
              $DOCKER_COMPOSE -f docker-compose.prod.yml up -d nginx
              sleep 5
            fi
            
            # Проверяем статус nginx (используем sudo если нужно)
            if sudo docker ps 2>/dev/null | grep -q nardist_nginx_prod || docker ps 2>/dev/null | grep -q nardist_nginx_prod; then
              echo "✅ Nginx container is running"
              # Проверяем что nginx действительно слушает на портах
              if sudo docker exec nardist_nginx_prod nginx -t 2>/dev/null || docker exec nardist_nginx_prod nginx -t 2>/dev/null; then
                echo "✅ Nginx configuration is valid"
              else
                echo "⚠️  Nginx configuration has errors, checking logs..."
                sudo docker logs nardist_nginx_prod --tail 50 2>/dev/null || docker logs nardist_nginx_prod --tail 50 2>/dev/null || true
              fi
              # Проверяем что порты заняты docker контейнером
              if lsof -i :80 2>/dev/null | grep -q docker || ss -tlnp | grep :80 | grep -q docker; then
                echo "✅ Port 80 is bound to docker nginx"
              else
                echo "❌ Port 80 is NOT bound to docker nginx! Checking what's using it..."
                lsof -i :80 2>/dev/null || ss -tlnp | grep :80 || true
              fi
            else
              echo "❌ Nginx failed to start, checking logs..."
              sudo docker logs nardist_nginx_prod --tail 50 2>/dev/null || docker logs nardist_nginx_prod --tail 50 2>/dev/null || true
              echo "🔄 Attempting to restart nginx..."
              sudo $DOCKER_COMPOSE -f docker-compose.prod.yml restart nginx 2>/dev/null || \
              $DOCKER_COMPOSE -f docker-compose.prod.yml restart nginx || \
              sudo $DOCKER_COMPOSE -f docker-compose.prod.yml up -d nginx 2>/dev/null || \
              $DOCKER_COMPOSE -f docker-compose.prod.yml up -d nginx
            fi
            
            # Применяем миграции (с таймаутом)
            timeout 60 sudo $DOCKER_COMPOSE -f docker-compose.prod.yml exec -T backend npx prisma migrate deploy 2>/dev/null || \
            timeout 60 $DOCKER_COMPOSE -f docker-compose.prod.yml exec -T backend npx prisma migrate deploy || echo "⚠️  Migrations failed or not needed"
            
            # Проверяем статус контейнеров
            echo "📊 Container status:"
            sudo $DOCKER_COMPOSE -f docker-compose.prod.yml ps 2>/dev/null || \
            $DOCKER_COMPOSE -f docker-compose.prod.yml ps || true
            
            # Очистка (не блокируем если долго)
            timeout 30 sudo docker system prune -f 2>/dev/null || timeout 30 docker system prune -f || true
            
            echo "✅ Deployment completed successfully!"

